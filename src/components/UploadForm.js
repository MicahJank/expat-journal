import React from 'react';

import useForm from '../utils/useForm.js';

const UploadForm = () => {

    return (
        <section>
            <div>
                Form
            </div>
        </section>
    )
}

export default UploadForm;
